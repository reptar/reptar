import {
  parse as fromFrontMatter,
  stringify as toFrontMatter
} from './front-matter';

import {
  parse as fromYaml,
  stringify as toYaml
} from './yaml';

export default {
  fromFrontMatter,
  toFrontMatter,

  fromYaml,
  toYaml,

  // fromJson
  // toJson

  // fromToml
  // toToml
};
