/**
 * YAML filename constants.
 * @enum {string}
 * @const
 */
exports.YAML = {
  CONFIG: '_config.yml',
  THEME: '_theme.yml'
};

/**
 * Key constants, used when looking up values in objects.
 * @enum {string}
 * @const
 */
exports.KEY = {
  SOURCE: 'source'
};
